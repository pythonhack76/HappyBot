import { DirettivaUnoDirective } from './direttiva-uno.directive';

describe('DirettivaUnoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirettivaUnoDirective();
    expect(directive).toBeTruthy();
  });
});
